name: CI Deep

# Exhaustive dynamic analysis. Monthly cron + on-demand ONLY -- never on a PR.
#
# The PR-time gates are the standalone workflows: fuzzing.yml (120s fuzz
# regression), valgrind.yml (60s memcheck lite) and security-scanners.yml. This
# file is their long-running counterpart: hours of fuzz discovery, 600s memcheck
# and helgrind soaks. Splitting them is what keeps the merge path fast while
# still running the heavy analysis somewhere.
#
# Jobs: build-flavors (nginx mainline/stable + angie) · fuzz (long) · memcheck
#   soak · helgrind soak.
#   memcheck AND helgrind run via ci/tools/soak.sh (USE_VALGRIND / USE_HELGRIND),
#   which passes --suppressions=ci/tools/valgrind.supp and --error-exitcode=99.
#
# build-flavors exists because build-test.yml (the fast PR gate) only builds
# ONE pinned nginx version -- the module claims angie support and tracks both
# nginx release branches, but nothing in CI actually built angie or nginx
# stable until this job. It is here (monthly), not in the PR gate, because
# each leg is a from-scratch configure+compile+Test::Nginx run and three of
# them would blow the PR gate's <15min budget.
#
# No chat notifications by design: a failure here surfaces as a red run + the
# uploaded crash/corpus artifacts + the job summary. Add a notify step only if
# someone is actually going to act on it -- an unread alert is worse than none.
#
# Action pins (keep in sync with build-test.yml):
# actions/checkout@v5        -> 93cb6efe18208431cddfb8368fd83d5badbf9bfd
# actions/upload-artifact@v5 -> 330a01c490aca151604b8cf639adc76d48f6c5d4

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  schedule:
    # Monthly, day 6 @ 03:30 UTC -- staggered from codeql.yml (day 4).
    - cron: "30 3 6 * *"
  workflow_dispatch:
    inputs:
      fuzz_seconds:
        description: "Fuzz duration (seconds)"
        required: false
        default: "3600"

concurrency:
  group: ci-deep-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-flavors:
    # matrix.label, not the version: the job name is evaluated before any step
    # runs, so the resolved version is not available here.
    name: Build & Test::Nginx (${{ matrix.flavor }} ${{ matrix.label }})
    runs-on: ${{ github.event.pull_request.head.repo.fork && 'ubuntu-latest' || fromJSON('["self-hosted","builder02","lxc"]') }}
    timeout-minutes: 30
    env:
      # This job's band, disjoint from build-test.yml's 19200/19300 and the
      # coverage job's 19400. `prove` binds a port, so the matrix legs need one
      # even though they never start the runtime driver; without it all three
      # fall back to Test::Nginx's hardcoded 1984.
      TEST_BASE_PORT: '19500'
    strategy:
      fail-fast: false
      # The three legs share ONE band, so they must not run at once: they are
      # pinned to the same host and a matrix is not serialised by default.
      # One band + max-parallel: 1 is cheaper than three disjoint bands and
      # keeps the band table readable.
      max-parallel: 1
      # A matrix is expanded before any step runs, so load-versions.sh cannot
      # feed it. Each cell therefore names the versions.env KEY it wants, and
      # the build step dereferences it after the pins are loaded -- keeping the
      # versions themselves in one file instead of re-hardcoding them here.
      matrix:
        include:
          - flavor: nginx
            version_key: NGINX_MAINLINE
            label: mainline
          - flavor: nginx
            version_key: NGINX_STABLE
            label: stable
          - flavor: angie
            version_key: ANGIE_VERSION
            label: angie
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Load version pins
        run: bash .github/scripts/load-versions.sh

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential curl libpcre2-dev zlib1g-dev \
            cpanminus libio-socket-ssl-perl
          sudo cpanm --notest --quiet Test::Nginx

      - name: Resolve matrix version
        # The cell names a versions.env key (NGINX_MAINLINE/NGINX_STABLE/
        # ANGIE_VERSION); dereference it now that load-versions.sh has put the
        # pins in the environment, so later steps can use env.MATRIX_VERSION.
        env:
          # Through env, not inline ${{ }}: an expression expanded straight into
          # a run: block is template injection (zizmor template-injection). The
          # matrix values are repo-controlled today, but the safe shape costs
          # nothing and stops the pattern from being copied somewhere it is not.
          MATRIX_VERSION_KEY: ${{ matrix.version_key }}
        run: |
          key="$MATRIX_VERSION_KEY"
          value="${!key:-}"
          if [ -z "$value" ]; then
            echo "::error::$key is not set in .github/versions.env"
            exit 1
          fi
          echo "MATRIX_VERSION=$value" >> "$GITHUB_ENV"
          echo "resolved $key=$value"

      - name: Restore build caches
        uses: ./.github/actions/build-cache
        with:
          mode: debug
          flavor: ${{ matrix.flavor }}
          nginx-version: ${{ env.MATRIX_VERSION }}

      - name: Build server and dynamic module
        # angie names its binary objs/angie, nginx names it objs/nginx. Assert
        # BOTH artifacts and export the server path, so the flavor's real binary
        # name lives in one place instead of being hardcoded per step -- checking
        # only the .so passes for angie while leaving the server unverified.
        env:
          MATRIX_FLAVOR: ${{ matrix.flavor }}   # via env, see the note above
        run: |
          set -euo pipefail
          bash ci/tools/ci-build.sh "$MATRIX_FLAVOR" "$MATRIX_VERSION"
          build=".build/$MATRIX_FLAVOR-${MATRIX_VERSION}-debug/objs"
          case "$MATRIX_FLAVOR" in
              angie) server="$build/angie" ;;
              *)     server="$build/nginx" ;;
          esac
          test -f "$build/ngx_http_skel_module.so"
          test -x "$server"
          echo "SERVER_BIN=$GITHUB_WORKSPACE/$server" >> "$GITHUB_ENV"
          echo "MODULE_SO=$GITHUB_WORKSPACE/$build/ngx_http_skel_module.so" >> "$GITHUB_ENV"

      # Above the FIRST binder (`prove` below). With max-parallel: 1 the legs
      # reuse one band in sequence, so this also catches a leg whose server
      # outlived its step and is still holding the port.
      - name: Verify this job's port band is free
        run: bash ci/tools/max-port.sh "$TEST_BASE_PORT"

      - name: Run ci/t/
        env:
          # Set by the build step above -- it resolves the flavor's binary name
          # (angie vs nginx) once, after the version is known.
          TEST_NGINX_BINARY: ${{ env.SERVER_BIN }}
          TEST_NGINX_LOAD_MODULES: ${{ env.MODULE_SO }}
          # See build-test.yml's identical setting for why this must be 20, not
          # the ~2s Test::Nginx::Socket default.
          TEST_NGINX_TIMEOUT: "20"
          # Without this the suite falls back to the hardcoded 1984, which is
          # what all three legs did before the band above existed.
          TEST_NGINX_PORT: ${{ env.TEST_BASE_PORT }}
          TEST_NGINX_SERVROOT: ${{ github.workspace }}/ci/t/servroot
        # Test::Nginx probes a server on PATH for its version and Bailouts if it
        # finds none -- TEST_NGINX_BINARY alone does not satisfy that probe, so
        # the objs/ dir goes on PATH too.
        run: PATH="$(dirname "$TEST_NGINX_BINARY"):$PATH" prove -v ci/t/

  fuzz:
    name: Fuzz (scan core, long)
    runs-on: ${{ github.event.pull_request.head.repo.fork && 'ubuntu-latest' || fromJSON('["self-hosted","builder02","lxc"]') }}
    timeout-minutes: 600
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Load version pins
        run: bash .github/scripts/load-versions.sh

      - name: Install build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential clang curl libpcre2-dev zlib1g-dev

      # The input goes through an env var, never ${{ }} inside run: -- template
      # expansion of event data into a shell script is an injection primitive
      # (zizmor template-injection), and this file is a template people copy.
      #
      # The env var closes template injection but NOT $GITHUB_ENV injection.
      # $GITHUB_ENV is parsed line by line, so a dispatch input containing a
      # newline ("1$'\n'PATH=/tmp/evil") writes a SECOND, attacker-chosen
      # variable into the environment of every later step in this job. The
      # value must therefore be validated before it is written, not merely
      # kept out of the shell's own syntax.
      #
      # Validation is whole-buffer, not per-line: `grep -qzE '^[0-9]{1,5}$'`
      # anchors against the entire NUL-delimited input, whereas a plain
      # `grep -qE` treats the embedded newline as a line boundary and matches
      # the benign first line -- letting the payload through.
      #
      # The LENGTH BOUND is load-bearing, not cosmetic. `[ "$v" -lt 1 ]` does
      # not return false on a value too large for the shell's integer type --
      # it ERRORS. Inside `if !` / `||` that failure is consumed, so with a
      # bare `^[0-9]+$` a 26-digit input passed the digit test, made both
      # arithmetic comparisons fail rather than compare, and was written to
      # $GITHUB_ENV unbounded. Five digits covers the 86400 ceiling and keeps
      # every accepted value inside the range the next check can actually
      # evaluate. (Caught by CodeRabbit on PR #18.)
      #
      # Then range-check, then fail the job LOUDLY. Never silently fall back
      # to the default on a rejected value: that turns an attack into a
      # normal-looking run. printf, not echo, avoids escape-sequence surprises.
      - name: Select fuzz duration
        env:
          DISPATCH_FUZZ_SECS: ${{ github.event.inputs.fuzz_seconds }}
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            if ! printf '%s' "${DISPATCH_FUZZ_SECS}" | grep -qzE '^[0-9]{1,5}$'; then
              echo "::error::fuzz_seconds must be 1-5 decimal digits (got invalid/empty/oversized input)"
              exit 1
            fi
            if [ "${DISPATCH_FUZZ_SECS}" -lt 1 ] || [ "${DISPATCH_FUZZ_SECS}" -gt 86400 ]; then
              echo "::error::fuzz_seconds out of range 1..86400 (got ${DISPATCH_FUZZ_SECS})"
              exit 1
            fi
            printf 'FUZZ_SECS=%s\n' "${DISPATCH_FUZZ_SECS}" >> "$GITHUB_ENV"
          else
            printf 'FUZZ_SECS=14400\n' >> "$GITHUB_ENV"
          fi

      - name: Restore build caches
        uses: ./.github/actions/build-cache
        with:
          mode: debug
          nginx-version: ${{ env.NGINX_VERSION }}

      - name: Fetch + configure nginx source (the fuzz build needs headers)
        run: bash ci/tools/ci-build.sh nginx "$NGINX_VERSION"

      - name: Build fuzz target
        run: CC=clang bash ci/fuzz/build.sh

      - name: Fuzz scan core
        run: |
          set -euo pipefail
          cd ci/fuzz
          echo "=== fuzz_scan (${FUZZ_SECS}s) ==="
          set +e
          ./fuzz_scan \
            -max_total_time="$FUZZ_SECS" \
            -max_len=16384 \
            -dict=fuzz.dict \
            -print_final_stats=1 \
            -artifact_prefix="crash-fuzz_scan-" \
            corpus/fuzz_scan
          rc=$?
          set -e
          [ $rc -eq 0 ] || { echo "❌ fuzz_scan crash (exit $rc)"; exit 1; }
          echo "✓ fuzz_scan: no crashes in ${FUZZ_SECS}s"
          # Minimize + persist the merged corpus so the next run starts from the
          # coverage this one found instead of from the committed seeds.
          mkdir -p corpus-min/fuzz_scan
          ./fuzz_scan -merge=1 corpus-min/fuzz_scan corpus/fuzz_scan || true
          echo "merged: $(find corpus-min/fuzz_scan -type f | wc -l) units" \
            >> "$GITHUB_STEP_SUMMARY"

      # On a crash this is the ONLY thing that makes the failure reproducible.
      # Commit the artifact into ci/fuzz/regressions/ as part of the fix, so
      # fuzzing.yml replays it forever after.
      - name: Upload crash artifacts
        if: failure()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: fuzz-deep-crashes
          path: ${{ github.workspace }}/ci/fuzz/crash-*
          retention-days: 30

      - name: Upload minimized corpus
        if: success()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: fuzz-corpus
          path: ${{ github.workspace }}/ci/fuzz/corpus-min
          retention-days: 30

  memcheck:
    name: Valgrind Memcheck soak
    runs-on: ${{ github.event.pull_request.head.repo.fork && 'ubuntu-latest' || fromJSON('["self-hosted","builder02","lxc"]') }}
    timeout-minutes: 240
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Load version pins
        run: bash .github/scripts/load-versions.sh

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential curl libpcre2-dev zlib1g-dev valgrind

      - name: Restore build caches
        uses: ./.github/actions/build-cache
        with:
          mode: debug
          nginx-version: ${{ env.NGINX_VERSION }}

      - name: Build debug nginx + module
        run: bash ci/tools/ci-build.sh nginx "$NGINX_VERSION" debug

      - name: Memcheck soak (600s @ 4 conc, WITH suppressions)
        env:
          USE_VALGRIND: "1"
        run: |
          bash ci/tools/soak.sh \
            "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}-debug/objs/nginx" 600 4

  helgrind:
    name: Valgrind Helgrind soak
    runs-on: ${{ github.event.pull_request.head.repo.fork && 'ubuntu-latest' || fromJSON('["self-hosted","builder02","lxc"]') }}
    timeout-minutes: 240
    steps:
      - name: Checkout module
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Load version pins
        run: bash .github/scripts/load-versions.sh

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential curl libpcre2-dev zlib1g-dev valgrind

      - name: Restore build caches
        uses: ./.github/actions/build-cache
        with:
          mode: debug
          nginx-version: ${{ env.NGINX_VERSION }}

      - name: Build debug nginx + module
        run: bash ci/tools/ci-build.sh nginx "$NGINX_VERSION" debug

      # Helgrind matters as soon as the module gains shared state (a shm zone, a
      # cache, a counter). With a purely per-request scan it finds nothing -- keep
      # it anyway, so the day shared state IS added the gate already exists.
      - name: Helgrind soak (600s @ 4 conc, --error-exitcode=99)
        env:
          USE_HELGRIND: "1"
        run: |
          bash ci/tools/soak.sh \
            "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}-debug/objs/nginx" 600 4

  # Coverage is a REPORT here, not a gate: nothing fails on a percentage.
  # Moving a number is cheap (write tests that touch lines and assert nothing)
  # and moving confidence is not, so the repo's coverage note is deliberate
  # about publishing the report and gating on the mutations recorded next to
  # each suite instead. Set COVERAGE_FAIL_UNDER to change that decision.
  #
  # It lives in ci-deep rather than in the PR gate because an instrumented build
  # is a THIRD build tree (-O0, no ccache reuse with debug or asan) plus a full
  # suite run -- minutes against a lane budget measured in seconds.
  coverage:
    name: Coverage report
    runs-on: ${{ github.event.pull_request.head.repo.fork && 'ubuntu-latest' || fromJSON('["self-hosted","builder02","lxc"]') }}
    timeout-minutes: 60
    env:
      # This job's port band, disjoint from build-test.yml's 19200. It runs
      # ci/tools/coverage.sh, which drives both the Test::Nginx suite and the
      # runtime driver; without a band both fall back to a shared default and
      # collide with whatever else is running on the host's six slots.
      TEST_BASE_PORT: '19400'
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          persist-credentials: false

      - name: Load version pins
        run: bash .github/scripts/load-versions.sh

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential curl libpcre2-dev zlib1g-dev \
            cpanminus libio-socket-ssl-perl gcovr
          sudo cpanm --notest --quiet Test::Nginx

      - name: Build instrumented, run every suite, report
        run: bash ci/tools/coverage.sh "$NGINX_VERSION"

      - name: Upload the coverage report
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: coverage-report
          path: .build/coverage
          if-no-files-found: error
